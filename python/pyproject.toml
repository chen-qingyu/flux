[build-system]
requires = ["setuptools>=68", "wheel>=0.43"]
build-backend = "setuptools.build_meta"

[project]
name = "flux"
version = "0.1.0"
description = "Python SDK for the flux simulator engine."
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["flux"]
